{
  "name": "capacitor-app",
  "version": "1.0.0",
  "description": "An Amazing Capacitor App",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-ios": "npx cap sync ios",
    "build-android": "npx cap sync android",
    "build-all": "npx cap sync",
    "open-ios": "npx cap sync ios && npx cap open ios",
    "open-android": "npx cap sync android && npx cap open android"
  },
  "keywords": [
    "capacitor",
    "mobile"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@byteowls/capacitor-oauth2": "git+https://github.com/tafelnl/capacitor-oauth2.git#v2.1.1",
    "@capacitor/android": "^2.5.0",
    "@capacitor/cli": "^2.5.0",
    "@capacitor/core": "^2.5.0",
    "@capacitor/ios": "^2.5.0"
  }
}
