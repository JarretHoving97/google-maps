name: iOS Staging Build

on:
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/ios-build.yml
    secrets: inherit
    with:
      app_frontend_url: https://qa.app.amigosapp.nl
      package_name: com.whoisup.app.qa
      environment: staging
